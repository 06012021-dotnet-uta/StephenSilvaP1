@model IEnumerable<WebStore.Models.Customer>

<div class="conntainner p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Customer List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Customer" asp-action="Create" class="btn btn-primary">
                Create New Customeer
            </a>
        </div>
    </div>
</div>

<br />
<br />
@if (Model.Count() > 0)
{
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Last Name
                </th>
                <th>
                    First Name
                </th>
            </tr>

        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="50%">@obj.FirstName</td>
                    <td width="50%">@obj.LastName</td>
                </tr>
            }
        </tbody>
    </table>
}




